#ifndef __DELAYS_H__
    #define __DELAYS_H__

    /* Definitions */
    #define COUNTS_PER_MS 8897

    /* Function prototypes */
    void sw_msDelay(unsigned int mS);
    void hw_msDelay(unsigned int mS);
#endif

